// import { Post } from "@prisma/client";

// export const getCommentCounter = async (posts: any[]) => {
//   const commentCount = posts.map((post) => ({
//     postId: post.id,
//     count: post.comments.length,
//   }));

//   // const muchPostIdCount = posts.map((post) => {
//   //   return post.id === commentCount[0].postId;
//   // });

//   return commentCount;
// };
